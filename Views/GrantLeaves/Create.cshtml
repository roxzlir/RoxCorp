@model RoxCorp.Models.GrantLeave

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>GrantLeave</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @* <div class="form-group">
                <label asp-for="FkApplyForLeaveId" class="control-label"></label>
                <select asp-for="FkApplyForLeaveId" class ="form-control" asp-items="ViewBag.FkApplyForLeaveId"></select>
            </div> *@
            <div class="form-group">
                <label asp-for="FkApplyForLeaveId" class="control-label">Application</label>
                <select asp-for="FkApplyForLeaveId" class="form-control" asp-items="@(new SelectList(ViewBag.ApplyForLeaveInfo, "ApplyForLeaveId", "DisplayText"))">
                    <option disabled selected>
                        -- Applications --
                    </option>
                </select>
            </div>

            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Granted" /> @Html.DisplayNameFor(model => model.Granted)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="DecisionDate" class="control-label"></label>
                <input asp-for="DecisionDate" class="form-control" />
                <span asp-validation-for="DecisionDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-success">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
