@model IEnumerable<RoxCorp.Models.GrantLeave>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ApplyForLeave)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Granted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DecisionDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ApplyForLeave.ApplyForLeaveId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Granted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DecisionDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GrantLeaveId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GrantLeaveId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GrantLeaveId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
